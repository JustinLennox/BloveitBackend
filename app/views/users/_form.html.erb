<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fbAccessToken %><br>
    <%= f.text_field :fbAccessToken %>
  </div>

  <div class="field">
    <%= f.label :favorites %><br>
    <%= f.text_field :favorites %>
  </div>
  <div class="field">
    <%= f.label :bloved_venues %><br>
    <%= f.text_field :bloved_venues %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
